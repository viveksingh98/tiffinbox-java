# Unit 05 — Variables and Types: Where Data Lives

- `Customer.java` — the four TiffinBox variables + `var total`, prints `Ravi eats 2 meals, pays 240.0`. Run: `java Customer.java`
- `BreakIt.java` — the "break it on purpose" file: `mealsPerDay = "two";` does NOT compile (`incompatible types: String cannot be converted to int`). Run: `java BreakIt.java` and read the error.
- Needs JDK 25 (compact source files + `IO.println`, JEP 512). Verified on JDK 25.0.4.1.
