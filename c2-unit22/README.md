# c2-unit22 — Strings Under the Hood: Concat, Interning, Unicode (JDK 25.0.4.1)
`Concat.java` / `Loop.java` are classic classes so `javap` has a `.class` to read: `javac -d . Concat.java && javap -c -p Concat.class` shows `label` as **four** bytecodes ending in `2: invokedynamic #7,  0   // InvokeDynamic #0:makeConcatWithConstants:(Ljava/lang/String;I)Ljava/lang/String;`, and `javap -v -p Concat.class` shows the bootstrap recipe `#37 \u0001 -> \u0001` calling `java/lang/invoke/StringConcatFactory.makeConcatWithConstants`. `javac -d . Loop.java && javap -c -p Loop.class` puts the same `invokedynamic` at offset **26**, between `20: aaload` and `35: goto 11` — inside the loop body. (3 runs each, byte-identical.)
`java --add-opens java.base/java.lang=ALL-UNNAMED Compact.java` reads `String`'s private `value` and `coder` and prints `"Ravi" … bytes=4 coder=0 (LATIN1)` / `"Meera Priya Ravi Sunil Kiran" … bytes=28` / `"Ravi ₹" … length()=6 bytes=12 coder=1 (UTF16)`. Add `-XX:-CompactStrings` and `"Ravi"` becomes `bytes=8`. **Without** `--add-opens` it exits 1 with `InaccessibleObjectException … module java.base does not "opens java.lang" to unnamed module` at `Compact.java:14`. (3 runs each, byte-identical.)
`java -Xms1g -Xmx1g -XX:+UseSerialGC Heap.java` → `used heap:      81 MB`; the same plus `-XX:-CompactStrings` → `used heap:      112 MB` (one million 32-character notes; **6 runs each, byte-identical**; the heap is pinned by the flags so you get the same two numbers). `java -XX:+PrintFlagsFinal -version | grep -i compactstring` → `bool CompactStrings = true {pd product} {default}`.
`java Intern.java` (10 runs, byte-identical) prints `a == b : true` / `a == c : false` / `a == folded ("Ra" + "vi") : true` / `a == built (part + "vi") : false` and three `intern()` lines, all `true`. `java Emoji.java` prints `length() : 2` / `codePointCount : 1` / `substring(0, 7) : [Priya ?]` / `cook length() : 5` / `cook codePointCount : 3` / `cook grapheme clusters : 1`. `java -Duser.language=en -Duser.country=US Locales.java` and `java -Duser.language=tr -Duser.country=TR Locales.java` differ on one line: `match against "PRIYA"` is `true` and then `false`. `java NumFmt.java` is the verified negative — `en-IN` prints `2,148,120`, identical to `en-US`; only the currency symbol differs.
`java Timing.java` is the honest one and it is **deliberately not reproducible in part**: the part-A stopwatch rounds ranged **4 ms to 22 ms** over eight runs with nothing changed, and the part-B warmed rounds ranged **3.7-12.9 ms fastest / 4.3-43.4 ms slowest**, so neither may be quoted as a fixed number. Part C is: `+ in a loop allocated : 59 MB` / `StringBuilder allocated: 96 KB` / `ratio : 633 times more` / `same string? : true`, **byte-identical on every run** (8 in the scratchpad, 3 more from this folder). `rm -f *.class` to clean up — `.gitignore` already covers them.
**Capture convention for this unit's md5s** (they are listed in the script's Director's Note; section-wide, `_briefs/OPEN-FINDINGS.md` #10; all re-captured under it 2026-09-14): `for i in 1 2 3; do <cmd> > NAME.$i.out 2>&1; echo $? > NAME.$i.exit; done; md5 -q NAME.*.out` — combined stdout+stderr only, exit code beside it, never folded into the hash. *(Earlier md5s in this unit appended a synthetic `exit=N` line, so they could not be reproduced from the stated recipe.)*
`Timing.java` part C gained a doubling sweep on 2026-09-14 (RED m4): `n = 5000 -> 59 MB` / `n = 10000 -> 238 MB` / `n = 20000 -> 954 MB` — ×4.03 then ×4.01, byte-identical on 8 runs (md5 of part C alone `75e5b0a9ef8d9bc5868e508e98cd1464`). The slide no longer asserts "quadruple the work"; it shows it.
