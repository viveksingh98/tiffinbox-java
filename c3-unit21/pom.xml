<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.tiffinbox</groupId>
  <artifactId>tiffinbox-core</artifactId>
  <version>1.0.0</version>
  <name>TiffinBox Core</name>
  <description>A log line a machine can query, and what that costs.</description>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- overridden per run by receipts.sh; see the exec plugin below -->
    <run.class>com.tiffinbox.kitchen.KitchenEvents</run.class>
    <run.arg>-Dlogback.dummy=unused</run.arg>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>6.1.3</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>2.5.250</version>
    </dependency>

    <!--
      logback-classic only, and slf4j-api comes with it - the logging unit before this one
      printed the resolved version rather than asserting it, and nothing here re-declares it.
    -->
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>1.6.3</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-jdk-platform-logging</artifactId>
      <version>2.0.19</version>
      <scope>runtime</scope>
    </dependency>

    <!--
      THE JSON ENCODER. Pinned from the version list on repo1.maven.org, where 9.0 is both
      the newest GA and the last entry - the <release> field agrees here, which it did not
      for slf4j-api.

      It is not free. `mvn dependency:tree` (./receipts.sh cost) shows it bringing THREE
      jars, and one of them arrives under a groupId you have probably never typed:
      tools.jackson.core, which is Jackson 3. Its annotations still come from Jackson 2's
      com.fasterxml.jackson.core. A project already on Jackson 2 therefore gets Jackson 3
      BESIDE it rather than in a fight with it - the groupId changed, so the two cannot
      collide. Print the tree; do not take that on trust.
    -->
    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>9.0</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.27.7</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.6.0</version>
      </plugin>
      <!-- exec:exec, not exec:java: every capture in this section needs a fresh JVM. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.6.4</version>
        <configuration>
          <executable>java</executable>
          <arguments>
            <argument>${run.arg}</argument>
            <argument>-classpath</argument>
            <classpath/>
            <argument>${run.class}</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
