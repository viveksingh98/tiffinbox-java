# c2-unit32 — Modules in Practice: exports, requires, ServiceLoader (JDK 25.0.4.1)
`s2/tiffinbox.api` is Unit 31's api module **plus** `Receipt` and `requires transitive java.net.http` — `Customer`, `MealType`, `Billing`, `PaymentGateway` and `internal/PriceCard` are the same files. `s2/` here is byte-identical to `c2-unit33/s2/`.
Build all four modules: `javac -d mods2 --module-source-path s2 $(find s2 -name '*.java')` — then `java --module-path mods2 -m tiffinbox.app/com.tiffinbox.app.Pay` → `gateways found: 2` + both charge lines + the receipt URI (3 runs byte-identical, md5 `8369830e…`). Copy only `mods2/tiffinbox.api` and `mods2/tiffinbox.app` into `mods2-min/` and run the same command → `gateways found: 0`, no recompile, no crash (md5 `a95363f6…`).
`java --module-path mods2 -m tiffinbox.app/com.tiffinbox.app.Reflect` → `InaccessibleObjectException` at `Reflect.java:13` (md5 `c2f26982…`). Three different ways to get `field value:       NON_VEG` (all md5 `398524a5…`): add `--add-opens tiffinbox.api/com.tiffinbox.api=tiffinbox.app` to that command; or swap in `breaks/opens-fix/module-info.java` as `s2/tiffinbox.api/module-info.java` and recompile; or leave the module system out of it entirely — `javac -d cp-out $(find s2/tiffinbox.api/com -name '*.java') s2/tiffinbox.app/com/tiffinbox/app/Reflect.java` then `java -cp cp-out com.tiffinbox.app.Reflect`.
`breaks/requires-not-transitive/module-info.java` replaces `s2/tiffinbox.api/module-info.java` to reproduce the two `requires transitive` compile errors (`Pay.java:27` and `Pay.java:28`). `breaks/private-constructor/CardGateway.java` replaces the card provider for `error: the no arguments constructor of the service implementation is not public: CardGateway`; `breaks/provider-factory/CardGateway.java` keeps the private constructor and compiles anyway — a `public static provider()` method is the documented second form, and it still prints md5 `8369830e…`.
`5400` = `PriceCard.pricePerMeal("NON_VEG")` 90 × 2 meals a day × 30 days, computed inside the non-exported `com.tiffinbox.api.internal` package from Unit 31. No flags, no preview, no dependencies.
