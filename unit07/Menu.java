void main() {
    String dish = "paneer butter masala";
    IO.println(dish.length());
    IO.println(dish.toUpperCase());
    IO.println(dish.substring(0, 6));
    IO.println(dish.contains("butter"));
    IO.println(dish);
    String shout = dish.toUpperCase();
    IO.println(shout);
}
