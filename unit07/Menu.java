void main() {
    String dish = "cottage cheese curry";
    IO.println(dish.length());
    IO.println(dish.toUpperCase());
    IO.println(dish.substring(0, 7));
    IO.println(dish.contains("cheese"));
    IO.println(dish);
    String shout = dish.toUpperCase();
    IO.println(shout);
}
