# Unit 39 — What's Next: Spring Boot, AI Agents and Your Java Path

- `Hello.java` — Unit 03's three lines, one more time (the course's closing panel): `void main() { IO.println("Hello, TiffinBox!"); }` prints `Hello, TiffinBox!`. Run: `java Hello.java`
- The finished TiffinBox console app (Units 37-38) lives in `../capstone/` — `cd ../capstone && mvn package && java -jar target/tiffinbox-1.0.jar`; its two known gaps (pauses are not saved; a comma in a name breaks the CSV split) are your first two commits.
- The road from here follows `docs/plan.md` of the course: Core Java II — Under the Hood → Build & Test Like a Pro → the complete Spring track (Spring Framework Core … Spring AI — complete) → the *Secure AI Agents with Java and Spring Boot* series on Prompt Vidya AI (https://www.youtube.com/@PromptVidyaAI).
- Needs JDK 25 (compact source files + `IO.println`, JEP 512). Verified on JDK 25.0.4.1.
