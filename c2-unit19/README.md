# c2-unit19 — Contracts: equals, hashCode and the Key That Vanished (JDK 25.0.4.1)
Run the compact source files directly (`export JAVA_HOME=/opt/homebrew/opt/openjdk@25; export PATH=$JAVA_HOME/bin:$PATH`) — all seven are 3/3 byte-identical, md5 in brackets.
`java EqualsOnly.java` [`6896ef73…`, exit 0] → `a.equals(b) : true` · `a.hashCode()==b.hashCode(): false` · `HashSet size : 2` · `set.contains(new Sub) : false` · `List.contains(new Sub) : true` · `HashSet of records size : 1` · `SubRecord.hashCode() : 78735104` · `Objects.hash("Ravi", 2) : 78736065` · and the merge block: `CHM.merge twice, size : 2` / `CHM values : [7200, 7200]` / `same with records, size : 1   values [14400]`. &nbsp;·&nbsp; `java BrokenKey.java` [`407db5d6…`, exit 0] → `hashCode when stored: 2539842` / `get(ravi): 7200` / `hashCode now: 1017123638` / `get(ravi): null` / `containsKey(ravi): false` / `remove(ravi): null` / `subs.size(): 1` / `subs.entrySet(): [Ravi Kumar(2)=7200]` / `get(new Ravi Kumar): null` / `get(new Ravi): null` — both `String.hashCode` values are specified in the Javadoc, so they are the same on every JVM.
`java TreeContract.java` [`e2933c8e…`, exit 0] → `ravi.equals(kiran): false` / `same bill: true` / `HashSet size: 2` / `TreeSet size: 1` / `TreeSet contents: [Customer[name=Ravi, mealsPerDay=2, pricePerMeal=120]]` / `tree.contains(kiran):true` / `tie-broken size: 2` / `book.get("ravi"): 7200` / `"ravi".equals("Ravi"): false`. &nbsp;·&nbsp; `java SortContract.java` [`5d76377b…`, **exit 1**] → three `BROKEN.compare` lines all `-1`, then `31 ratings sorted: no exception, stars now 1111111111122222222223333333333`, then `IllegalArgumentException: Comparison method violates its general contract!` at `TimSort.mergeHi(TimSort.java:903)` … `SortContract.main(SortContract.java:31)`.
`java Hunt.java` [`dcf6954b…`, exit 0] measures the threshold instead of quoting it — 1000 random inputs per size, fixed seed: `0` throws at n = 2, 8, 16, 24, **31**; `106` at **32**; `144` at 33; `443` at 40; `979` at 64; `1000` at 100 (1-5 stars) — and the 2…200 sweep reports `first size that ever throws: 32`, which is `TimSort.MIN_MERGE` (JDK source `java.base/java/util/TimSort.java:80`, used at `:219`).
`java Guards.java` [`f1f4bf1f…`, exit 0] → `assertions enabled:  false` / `past the assert, slot = 3` / `checkIndex: …IndexOutOfBoundsException: Index 3 out of bounds for length 3` / `requireNonNull: customer name must not be null` / `compact ctor: mealsPerDay must be 1..3, got 5` / `helpful NPE: Cannot invoke "String.length()" because "<local3>" is null`; `java -ea Guards.java` [`493ecf11…`, **exit 1**] → `assertions enabled:  true` then `AssertionError: menu slot out of range: 3` at `Guards.java:22`; and `javac -g -d out Guards.java && java -cp out Guards` [`eee51a11…`] turns that last line into `because "missing" is null` — the variable name needs `-g`.
`java Boxing.java` [`1d81e409…`, **exit 1**] → `Integer a = 127, b = 127;  a == b : true` but `Integer c = 128, d = 128;  c == d : false`, `c.equals(d) : true`, `meal counts  2 == 2 : true`, `bills 7200 == 7200 : false`, `bills .equals : true`, then an uncaught `NullPointerException: Cannot invoke "java.lang.Integer.intValue()" because the return value of "java.util.HashMap.get(Object)" is null` at `Boxing.java:23` (fix: `getOrDefault("Kiran", 0)`). The **same file** under `java -XX:AutoBoxCacheMax=10000 Boxing.java` [`7a1ec6f6…`, **exit 1**] flips both to `true`, while `javac -Xlint:all -d out Boxing.java` exits **0** with zero bytes of output and `-XX:+PrintFlagsFinal -version` reports `intx AutoBoxCacheMax = 128 {C2 product} {default}`. *(Moved here from `c2-unit21/` on 2026-09-14: `==` versus `.equals` is this unit's subject.)*
**Capture convention for every md5 above** (section-wide, `_briefs/OPEN-FINDINGS.md` #10): `for i in 1 2 3; do <cmd> > NAME.$i.out 2>&1; echo $? > NAME.$i.exit; done; md5 -q NAME.*.out` — the hash covers combined stdout+stderr only; the exit code sits beside it and is never folded in.
