// Gradle reads this file before anything else. It names the build; without it, the name
// of the directory on disk would become the project name - and the jar would be called
// after the folder instead of after the module.
rootProject.name = "tiffinbox-core"
