# c2-unit23 — Collections Beyond List and Map: Deque, PriorityQueue, EnumMap (JDK 25.0.4.1)
Four compact source files, no flags, no dependencies: `export JAVA_HOME=/opt/homebrew/opt/openjdk@25; export PATH=$JAVA_HOME/bin:$PATH`, then run each with `java <File>.java`. Every capture below is 3/3 runs byte-identical on JDK 25.0.4.1 (macOS, Apple silicon, 2026-09-14).
`java Shapes.java` (exit 0, md5 `10052f8331d5ebab3fab30cf710d7978`) — `deque : [Kiran, Meera, Priya, Ravi]` / `peekFirst on empty: null` / `addLast(null) : NullPointerException` / `ArrayDeque, push : [price, address, meal]` vs `java.util.Stack : [meal, address, price]` (both pop `price`) / `added in order : [Meera(20), Kiran(12), Ravi(25), Sunil(8), Priya(5)]` but `printed : [Priya(5), Sunil(8), Ravi(25), Meera(20), Kiran(12)]` and `polled : [Priya(5), Sunil(8), Kiran(12), Meera(20), Ravi(25)]` — **three different orders** / `floorKey(765) : 750`, `ceilingKey(765) : 780`, `headMap(780) : {720=Meera, 750=Priya}` then `same headMap now : {720=Meera, 750=Priya, 765=Kiran}` (a live view) / `put order : [VEGAN, VEG, NON_VEG]` but `EnumMap : {VEG=11700, NON_VEG=9000, VEGAN=3600}` / `EnumSet class : java.util.RegularEnumSet` and `over 64 constants : java.util.JumboEnumSet`. Run `javap java.util.Stack` beside it to see `extends java.util.Vector` and `public synchronized E pop();`.
`java OrderRail.java` (**exit 1 on purpose**, md5 `0d3af376d33f2168c3cf5a712ceb93e9`) — the hand-written `Iterable` prints `meals on the rail : 7` / `a second walk : 4 slips (a fresh Iterator each time)` / `past the end : NoSuchElementException: rail is empty`, then the loop everybody writes first throws `java.util.ConcurrentModificationException` at `ArrayList$Itr.checkForComodification(ArrayList.java:1096)` → `ArrayList$Itr.next(ArrayList.java:1050)` → `OrderRail.main(OrderRail.java:50)`.
`java CmeSilent.java` (exit 0, md5 `68acfc473461d70573f5cb6217f77866`) — the same bug with the **second-to-last** element removed: `removed 'Ravi' (2nd from last) : no exception` / `elements the loop visited : 3 of 4` / `'Sunil' was never visited : true`, then the two fixes (`removeIf`, `Iterator.remove`, both `[Meera, Ravi, Sunil]`) and the impostor `java.util.Arrays$ArrayList` (`set` writes through to the array, `add` throws `UnsupportedOperationException`).
`java Sequenced.java` (exit 0, md5 `b853417ff2569c476204d6331e8464c9`) — `getFirst()`/`getLast()`/`reversed()` on a `List`, a `LinkedHashSet` and a `LinkedHashMap` (Java 21+), then `binarySearch(5000) : -3` (`-(insertion point) - 1`), `Arrays.equals : true` while `bills.equals(same) : false`, and `Arrays.mismatch : 1`. **`EnumHash.java` is evidence, not teaching material** — its `HashMap`-with-enum-keys order matched `EnumMap` on 8/8 runs, which is an identity-hash accident; never put that output on a slide.
