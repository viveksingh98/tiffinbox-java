# c2-unit20 — Immutability and the Defensive Copy You Forgot (JDK 25.0.4.1, no flags)
`java Leaky.java` → `built: … [VEG, VEG, NON_VEG]` / `after caller add: … VEGAN]` / `after getter add: … VEGAN, VEG]` / `in a HashSet: true` / `after one mutate: false` / `hashCode before: -139842468` / `hashCode after: -345838620` / `Objects.hash(..): -345837659` (a record's `hashCode` is **not** `Objects.hash`, it is 961 = 31² away) / `safe, caller add: SafeSubscription[customer=Meera, meals=[VEG, VEG, NON_VEG]]` / `safe, getter add: UnsupportedOperationException (message: null)` — 3/3 runs byte-identical, md5 `acef1d94…`.
`java CopyVsView.java` → `view: [Meera, Priya, Ravi, Sunil]  copy: [Meera, Priya, Ravi]` / `view class: java.util.Collections$UnmodifiableRandomAccessList` / `copy class: java.util.ImmutableCollections$ListN` / `copyOf(copy) == copy : true` / `copyOf(view) == view : false` / `List.copyOf(list with null) -> NullPointerException` / `Set.of duplicate -> java.lang.IllegalArgumentException: duplicate element: VEG` (md5 `b48dedd2…`) · `java ArrayRecord.java` → `a.equals(b) : false` / `a.toString() : Week[customer=Ravi, mealsPerDay=[I@<addr>]` (identity hash — yours will differ) / `SafeWeek after both : [2, 2, 2, 2, 2, 2, 2]` (md5 `af7268df…`) · `java Deep.java` → `String : Meera` / `LocalDate : 2026-09-14` / `BigDecimal : 120.00` / `List.of : [VEG, NON_VEG]  add -> UnsupportedOperationException` (md5 `de5d82e9…`).
Compact **is** canonical: `javac -d cout Ctors.java` then `javap -c -p 'cout/Ctors$Compact.class' | sed -n '/Compact(java.lang.String, int);/,/29: return/p' | tail -n +2 > a.txt`, the same for `Ctors$Canonical` into `b.txt`, then `diff a.txt b.txt; echo $?` → **0** (18 identical lines, constant pool `#1 #7 #9 #11 #14 #20`, 3/3 runs) · `cd bad && javac -d . Bad.java` → `Bad.java:4: error: cannot assign a value to final variable meals` + caret + `1 error` (md5 `e3292d4e…`, exit 1) — inside a compact constructor you assign to the **parameter**.
The `MatchException` needs a real two-step compile — both source states ship here: `cd sealed && mkdir -p out out2` · **step 1** `javac -implicit:none -d out v1/Meal.java v1/Veg.java v1/NonVeg.java && javac -implicit:none -cp out -d out app/Pricing.java && java -cp out Pricing` → `today: Veg[name=Garden Bowl, price=120]` / `fee:   20` (md5 `cb7a3d97…`, exit 0) · **step 2, rebuild only the library** `javac -implicit:none -d out v2/Meal.java v2/Veg.java v2/NonVeg.java v2/Vegan.java && md5 out/Pricing.class && java -cp out Pricing` → `Pricing.class` is still md5 `4ce0baa65c74b78957bbb26fefe53ed1`, and it now prints `today: Vegan[…]` then `Exception in thread "main" java.lang.MatchException` / `at Pricing.deliveryFee(Pricing.java:3)` / `at Pricing.main(Pricing.java:12)` — the whole 2-frame trace, nothing trimmed (md5 `2094f9d2…`, exit 1); `javac -implicit:none -cp out -d out app/Probe.java && java -cp out Probe` reads it out: `message: null` / `cause: null` / `frames: 2` (md5 `f4a458d9…`) · **step 3** `javac -implicit:none -cp out -d out2 app/Pricing.java` → `app/Pricing.java:3: error: the switch expression does not cover all possible input values` + caret + `1 error` (md5 `30d54727…`, exit 1). `-implicit:none` everywhere so javac cannot quietly rebuild the caller and hide the demo.
`java Gap.java` [`1c4594ba…`, exit 0] answers the arithmetic question Unit 19 raises and this unit only back-points to: the gap between a record's generated `hashCode` and `Objects.hash(components)` is **31 to the power of the component count**, not a constant — `1 component : gap 31` / `2 components : gap 961` / `3 components : gap 29791` / `4 components : gap 923521`, printed beside `31^1 … 31^4`. `Objects.hash` seeds its loop at `1`, this JDK's generated method seeds at `0`. **A record's hash value is unspecified** — these numbers are measured on this build, not promised by the spec, so never hand-write one to match the other and never persist either. 3/3 runs byte-identical.
