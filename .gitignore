*.class
.idea/
out/
target/
.DS_Store

# ---------------------------------------------------------------- Course 1 ---
# generated by the Course 1 examples (the capstone customers.csv IS committed)
unit24/customers.csv
unit26/customers.csv
unit27/customers.csv

# generated by Unit 44 (javadoc output - regenerate with the README's commands)
unit44/docs/
unit44/brokendocs/
unit44/slipdocs/
unit44/compact/docs/
unit44/exercise/docs/
unit44/exercise/starter/docs/

# ---------------------------------------------------------------- Course 2 ---
# Anything below is produced by a command in some c2-unitNN/README.md.
# verify_course2.sh removes all of it as well; these lines are the second net.

# heap dumps, flight recordings, thread dumps and the logs the demos redirect
*.hprof
*.jfr
threads.json
threads-*.json
server.log

# H2 writes its database beside the unit that opened it (units 34-38)
data/
*.mv.db
*.trace.db

# mvn dependency:build-classpath -Dmdep.outputFile=cp.txt (units 29, 30, 37, 38)
cp.txt

# left behind on purpose by c2-unit25/WrongCharset.java (it crashes before its delete line)
c2-unit25/tiffinbox-charset-*/
c2-unit25/tiffinbox-*/
c2-unit26/tiffinbox-*/
c2-unit26/menus/

# c2-unit20: the javap bytecode diff (compact vs canonical) and the two-step sealed build
c2-unit20/cout/
c2-unit20/sealed/out/
c2-unit20/sealed/out2/

# c2-unit24: every class-loading demo compiles into its own tree.
# These names are scoped to the unit on purpose — `app/` is a real source
# directory in c2-unit20, c2-unit31, c2-unit32, c2-unit33 and unit45.
c2-unit24/fake/
c2-unit24/o2/
c2-unit24/app/
c2-unit24/plugins/
c2-unit24/nd/

# c2-unit31 / 32 / 33: module builds, the automatic-module jar and the broken copies
c2-unit31/mods/
c2-unit31/modsonly/
c2-unit31/plainout/
c2-unit31/lib/
c2-unit31/mf.txt
c2-unit31/bad1/
c2-unit31/bad2/
c2-unit32/mods2/
c2-unit32/mods2-min/
c2-unit32/cp-out/
c2-unit33/mods2/
c2-unit33/modsd/
c2-unit33/plainout/
c2-unit33/lib/

# c2-unit33: jlink runtime images and jpackage bundles.
# NOT `*.app/` — on a case-insensitive filesystem that also matches the
# `tiffinbox.app` module directory in units 31/32/33 and hides real source.
image2/
imagex/
dist/
dmgout/
dist/TiffinBox.app/
*.dmg

# ---------------------------------------------------------------- Course 3 ---
# Produced by a command in a c3-unitNN/README.md, and machine-specific, so the
# `git add c3-*` that first commits Course 3 must not sweep them in.
# `target/` (incl. c3-unit06's target/team-repo) is covered at the top of this
# file; `.m2-demo/` and `.m2-unit06/` by c3-unit02/.gitignore + c3-unit06/.gitignore.
# Scoped to their units, like the c2-unit24 block above, and kept here even
# where a per-unit .gitignore repeats them: same second net as Course 2.

# c3-unit01 README cmd 1: help:effective-pom -Doutput=effective-pom.xml
c3-unit01/effective-pom.xml

# c3-unit03 README cmd 5: dependency:build-classpath -Dmdep.outputFile=cp-$s.txt
# (absolute paths into whichever local repository resolved the jars)
c3-unit03/scopes/cp-*.txt

# c3-unit06 README cmd 1: help:effective-settings -Doutput=es.xml
# (its own README: "the file's header comment names your user and host")
c3-unit06/es.xml

# --------------------------------------------- Course 3 · Section 2 (Gradle) ---
# Everything below is produced by a command in a c3-unit07..10 README (or by that
# unit's receipts.sh). Scoped per unit, exactly like the Course 2 and Course 3
# blocks above, because `build/` is a plausible source directory name and a bare
# `build/` here would hide one in some future unit.
#
# NOT ignored, and deliberately committed: gradlew, gradlew.bat,
# gradle/wrapper/gradle-wrapper.jar, gradle/wrapper/gradle-wrapper.properties,
# mvnw, mvnw.cmd and .mvn/wrapper/maven-wrapper.properties. The wrapper being in
# the repository IS the lesson of unit 10 - a .gitignore that swallowed it would
# contradict the unit it belongs to.

# Gradle's per-project state: the incremental-build database and the file hashes
# behind every UP-TO-DATE in unit 08.
c3-unit07/.gradle/
c3-unit08/.gradle/
c3-unit09/.gradle/
c3-unit10/.gradle/
c3-unit07/exercise/.gradle/
c3-unit08/exercise/.gradle/
c3-unit09/exercise/.gradle/
c3-unit10/exercise/.gradle/
c3-unit08/breaks/undeclared-input/.gradle/
c3-unit09/conflict/.gradle/

# GRADLE_USER_HOME="$PWD/.gradle-home" - line 3 of every README in this section.
# Holds the downloaded 9.7.1 distribution (~130 MB), the dependency cache, the
# build cache and the daemon registry. Deleting it leaves nothing of this course
# on the machine; it must never be committed.
c3-unit07/.gradle-home/
c3-unit08/.gradle-home/
c3-unit09/.gradle-home/
c3-unit10/.gradle-home/

# Throw-away Gradle homes: unit 10's sha256 demo and its older-distribution break,
# and the same demo in unit 10's exercise.
c3-unit10/.gh-fresh/
c3-unit10/.gh-old/
c3-unit10/exercise/.gh-fresh/

# Gradle's output directory. `target/` is already covered at the top of this file;
# this is its Gradle twin, and it also holds build/generated/menu (unit 08's
# generated MenuCatalog.java) and build/libs/lib (unit 10's five runtime jars).
c3-unit07/build/
c3-unit08/build/
c3-unit09/build/
c3-unit10/build/
c3-unit09/tiffinbox-core/build/
c3-unit09/tiffinbox-web/build/
c3-unit10/tiffinbox-core/build/
c3-unit10/tiffinbox-web/build/
c3-unit07/exercise/build/
c3-unit08/exercise/build/
c3-unit09/exercise/build/
c3-unit09/exercise/tiffinbox-core/build/
c3-unit09/exercise/tiffinbox-web/build/
c3-unit10/exercise/build/
c3-unit08/breaks/undeclared-input/build/
c3-unit09/conflict/build/

# -Dmaven.repo.local="$PWD/.m2-demo" - the isolated local repository every Maven
# command in this section uses so that ~/.m2 is never the subject. In unit 10 it
# also holds the Maven distribution ./mvnw downloads under MAVEN_USER_HOME.
c3-unit07/.m2-demo/
c3-unit09/conflict/.m2-demo/
c3-unit10/.m2-demo/

# Not ignored on purpose: c3-unit08/src/main/menu/menu.txt and
# c3-unit08/breaks/undeclared-input/src/main/menu/menu.txt. Both are committed with
# THREE lines. The README and receipts.sh append a fourth and put it back; if a run
# is interrupted, `git diff` on those two files is what tells you, which is the
# whole reason they are tracked.

# ------------------------------ Course 3 · Section 3 (Testing That Earns Trust) ---
# Produced by a command in a c3-unit11..18 README or by that unit's receipts.sh.
# Each of those eight units carries its own .gitignore with the same rules; these
# are the second net, the same convention as the Course 2 and Section 1-2 blocks
# above. Scoped per unit on purpose — `.sol/`, `.fix/`, `.q/` and friends are
# throwaway build copies, and an unscoped rule would match real directories.
c3-unit11/.m2-demo/
c3-unit11/.sol/
c3-unit11/.green/
c3-unit11/.green2/
c3-unit11/.capstone/
c3-unit11/.r-cp.txt
c3-unit12/.m2-demo/
c3-unit12/.sol/
c3-unit12/.zero-strict/
c3-unit12/.assertall/
c3-unit12/.noagent/
c3-unit12/.space trap/
c3-unit13/.m2-demo/
c3-unit13/.sol/
c3-unit13/.zero-strict/
c3-unit13/.noagent/
c3-unit13/.space trap/
c3-unit14/.m2-demo/
c3-unit14/.sol/
c3-unit14/.zero-strict/
c3-unit14/.noagent/
c3-unit14/.space trap/
c3-unit14/.amount/
c3-unit14/.flags/
c3-unit15/.m2-demo/
c3-unit15/.sol/
c3-unit15/.q/
c3-unit15/.space trap/
c3-unit15/.ex/
c3-unit15/.r-cp.txt
c3-unit16/.m2-demo/
c3-unit16/.sol/
c3-unit17/.m2-demo/
c3-unit17/.sol/
c3-unit17/.fix/
c3-unit18/.m2-demo/
c3-unit18/.sol/
c3-unit18/.props/

# The receipts.sh working files, in all eight. `.r-*.out` is the capture a block
# hashes and `.r-*.raw` the unfiltered build log it came from; both are rebuilt by
# every run, and neither belongs in a clone.
c3-unit1[1-8]/.r-*.out
c3-unit1[1-8]/.r-*.raw

# ------------------------------- Course 3 · Section 4 (Logging & Measurement) ---
# Everything below is produced by a command in a c3-unit19..22 README, or by that
# unit's receipts.sh. Scoped per unit, exactly like the Course 2 and Section 2
# blocks above, because `logs/` and `central/` are plausible source directory
# names and a bare rule here would hide one in some future unit.
#
# NOT ignored, and deliberately committed: c3-unit19/central/ and
# c3-unit22/central/ - the maven-metadata.xml and the Central directory listing
# each unit's `receipts.sh release` derives its version chip from, with no
# network. They are evidence, and a .gitignore that swallowed them would make
# that block unrunnable from a clean clone.

# receipts.sh working files: one .out per block, the .raw capture behind it, and
# the throwaway trees the exercise answers and the probes are run in.
c3-unit19/.r-*.out
c3-unit19/.r-*.raw
c3-unit19/.r-cp.txt
c3-unit19/.sol/
c3-unit20/.r-*.out
c3-unit20/.r-*.raw
c3-unit20/.r-cp.txt
c3-unit20/.sol/
c3-unit20/.gate/
c3-unit21/.r-*.out
c3-unit21/.r-*.raw
c3-unit21/.r-cp.txt
c3-unit21/.sol/
c3-unit22/.r-*.out
c3-unit22/.r-*.raw
c3-unit22/.r-cp.txt
c3-unit22/.r-cc.txt
c3-unit22/.sol/

# -Dmaven.repo.local="$PWD/.m2-demo" - the isolated local repository every Maven
# command in this section uses, so that ~/.m2 is never the subject. `target/` is
# already covered at the top of this file, and it is where every log file, every
# rolled archive and every JMH jar below actually lands.
c3-unit19/.m2-demo/
c3-unit20/.m2-demo/
c3-unit21/.m2-demo/
c3-unit22/.m2-demo/

# The log trees the appender unit and the structured-logging unit write into.
# target/ already covers target/logs, but ${logdir} is overridable on the command
# line and a viewer who points it at the project root should not commit the result.
c3-unit20/logs/
c3-unit21/logs/
c3-unit22/logs/

# Rolled archives, wherever they land: kitchen.1.log .. kitchen.3.log are produced
# by the rolling appender and by the exercise's answer.
c3-unit20/kitchen*.log
c3-unit20/exercise/kitchen*.log
c3-unit21/kitchen*.log
c3-unit21/kitchen*.json

# JMH: the shaded uber-jar the benchmarks run from, and every result format its
# -rf flag can write. The jar is 5.5 MB and is rebuilt by `mvn package`.
c3-unit22/benchmarks.jar
c3-unit22/exercise/benchmarks.jar
c3-unit22/breaks/no-blackhole/benchmarks.jar
c3-unit22/jmh-result.*
c3-unit22/exercise/jmh-result.*

# JFR recordings need no rule here: the Course 2 block above already carries a
# bare `*.jfr`, which covers c3-unit22/target/bench.jfr and anything a viewer
# records beside it. Repeating it would be the second net twice.

# ------------------------------- Course 3 · Section 5 (Ship the Artifact) ------
# Everything below is produced by a command in a c3-unit23..28 README, or by that
# unit's receipts.sh. Scoped per unit, exactly like the Section 2, 3 and 4 blocks
# above, because `native/`, `central/`, `wrapper/` and `templates/` are all
# plausible source directory names and a bare rule here would hide one in some
# future unit.
#
# NOT ignored, and deliberately committed:
#   c3-unit23/central/          the maven-jar-plugin metadata and directory listing
#                               `./receipts.sh release` derives its version chip from
#   c3-unit25/central/          the four GitHub action release documents and the
#                               Adoptium release list `./receipts.sh actions` reads
#   c3-unit27/central/          the native-maven-plugin metadata and the GraalVM CE
#                               release document `./receipts.sh graalvm` reads
#   c3-unit24/wrapper/          a REAL gradle-wrapper.jar, 47 KB of binary, which is
#                               the whole point of that unit's second break. The root
#                               of this file has no `*.jar` rule and must never grow
#                               one - see c3-unit24/templates/java.gitignore.
#   c3-unit25/workflows/        the taught workflow file. It is NOT at .github/ and
#                               must not be moved there by a rule.
#   c3-unit27/src/main/resources/META-INF/native-image/   reflect-config.json and
#                               resource-config.json, which are evidence, not output.
# All of those are tracked, and `git check-ignore -q` must keep returning non-zero
# for every one of them or `./receipts.sh` stops working from a clean clone.

# receipts.sh working files: one .out per block, the .raw build log behind it, the
# de-duplicated finding lists two blocks derive their counts from, and the
# throwaway trees the exercise answers and the probes run in.
c3-unit23/.r-*.out
c3-unit23/.r-*.raw
c3-unit23/.r-layers.hashed
c3-unit23/.sol/
c3-unit23/.mi1/
c3-unit23/.mi2/
c3-unit23/.mi3/
c3-unit24/.r-*.out
c3-unit24/.r-*.raw
c3-unit25/.r-*.out
c3-unit25/.r-*.raw
c3-unit25/.sol/
c3-unit26/.r-*.out
c3-unit26/.r-*.raw
c3-unit26/.r-*.uniq
c3-unit26/.sol/
c3-unit26/.spot/
c3-unit27/.r-*.out
c3-unit27/.r-*.raw
c3-unit28/.r-*.out
c3-unit28/.r-*.raw

# -Dmaven.repo.local="$PWD/.m2-demo" - the isolated local repository every Maven
# command in this section uses, so that ~/.m2 is never the subject. `target/` is
# already covered at the top of this file, and it is where every jar, every uber-jar
# and every surefire report below actually lands.
c3-unit23/.m2-demo/
c3-unit24/.m2-demo/
c3-unit25/.m2-demo/
c3-unit26/.m2-demo/
c3-unit27/.m2-demo/
c3-unit28/.m2-demo/

# The throwaway git repositories unit 24 creates, and destroys with
# `./receipts.sh clean`. They are nested repositories: without this rule git would
# offer to add them as gitlinks, which is a different and much more confusing mess.
c3-unit24/.repos/

# The case-sensitive disk image unit 25 makes with hdiutil to reproduce, locally,
# the failure a Linux runner would report. It is 64 MB and it is deleted by the
# block that makes it - this rule is for the run that was interrupted.
c3-unit25/.r-cs.dmg

# Error Prone's build under the break project, which has its own target/ one level
# deeper than the rule at the top of this file reaches.
c3-unit26/breaks/no-jvm-config/target/

# The AOT configuration and cache units 27 and 28 record and create. Both are ~20 MB
# and both are a function of THIS machine's JVM - an AOT cache is not portable and
# must never be committed.
c3-unit27/.aot.conf
c3-unit27/.aot.cache
c3-unit27/.aot.dir.conf
c3-unit28/.aot.conf
c3-unit28/.aot.cache

# The jlink runtime images units 27 and 28 build. 31-37 MB each, produced in one
# command from the JDK you already have.
c3-unit27/.img-full/
c3-unit27/.img-trim/
c3-unit28/.img/

# The exercise builds: unit 23's exercise/ is a second Maven project and produces a
# target/ of its own, one level below the root rule.
c3-unit23/exercise/target/

# ---------------------------------------------- Course 4 · Section 1 (The Container) ---
# Spring Framework Core, units 01-06. Scoped per unit, exactly like the Course 2 and
# Course 3 blocks above, so a rule can never reach a folder it was not written for.
#
# What is DELIBERATELY NOT ignored, and must stay tracked, because a clean clone has to be
# able to run `ContextReport` and read the evidence: every src/ tree, every pom.xml, every
# README.md, every exercise/solution/*, c4-unit01/ledger.sh, c4-unit01/boot-in-ninety-seconds/
# (the one Boot pom in the course), c4-unit02/legacy/applicationContext.xml and the copy of it
# under src/main/resources/legacy/, chain.py where a unit ships one, and every breaks/ project.

# -Dmaven.repo.local="$PWD/.m2-demo" - the isolated local repository every Maven command in
# this section uses, so that ~/.m2 is never the subject. `target/` is already covered at the
# top of this file. boot-in-ninety-seconds keeps its OWN repository because it resolves a
# parent (spring-boot-starter-parent) the rest of the section does not hold.
c4-unit01/.m2-demo/
c4-unit01/boot-in-ninety-seconds/.m2-demo/
c4-unit02/.m2-demo/
c4-unit03/.m2-demo/
c4-unit04/.m2-demo/
c4-unit05/.m2-demo/
c4-unit06/.m2-demo/
c4-tiffinbox/.m2-demo/

# The resolved runtime classpath, written by `dependency:build-classpath` so the java -cp line
# in each README is one short command. It is a file full of absolute paths into .m2-demo, so it
# is machine-local by construction and must never be committed.
c4-unit01/.cp
c4-unit02/.cp
c4-unit03/.cp
c4-unit04/.cp
c4-unit05/.cp
c4-unit06/.cp

# Capture working files: one .out per named capture, the .raw behind it (a raw run still
# carries the JUL timestamp line, which is why the .out beside it exists), and the .exit file
# holding that run's exit code. Three of each, per capture, per the three-runs rule.
c4-unit01/.r-*
c4-unit01/exercise/.r-*
c4-unit01/boot-in-ninety-seconds/.r-*
c4-unit01/breaks/one-new-deleted/.r-*
c4-unit01/breaks/one-bean-deleted/.r-*
c4-unit02/.r-*
c4-unit02/exercise/.r-*
c4-unit03/.r-*
c4-unit03/exercise/.r-*
c4-unit04/.r-*
c4-unit04/exercise/.r-*
c4-unit04/breaks/one-segment-wrong/.r-*
c4-unit05/.r-*
c4-unit05/exercise/.r-*
c4-unit06/.r-*
c4-unit06/exercise/.r-*
c4-unit06/breaks/primary-on-both/.r-*

# The shared TiffinBoxConfig every unit after the first starts from. It is an authoring
# convenience in unit 01's folder, not a file any unit compiles.
c4-unit01/.shared-TiffinBoxConfig.java

# Second-level build output. `target/` at the top of this file does not reach a project
# nested inside a unit, and every exercise/ and breaks/ project here is its own Maven project.
c4-unit01/exercise/target/
c4-unit01/boot-in-ninety-seconds/target/
c4-unit01/breaks/one-new-deleted/target/
c4-unit01/breaks/one-bean-deleted/target/
c4-unit02/exercise/target/
c4-unit03/exercise/target/
c4-unit04/exercise/target/
c4-unit04/breaks/one-segment-wrong/target/
c4-unit05/exercise/target/
c4-unit06/exercise/target/
c4-unit06/breaks/primary-on-both/target/

# The module-path probe unit 04's module slide shows (`c4-unit04/modpath/`). It is NOT a Maven
# project: `run.sh` compiles the module with `javac -d out` off the `.cp` one level up, and writes
# one `.out` and one `.exit` per run, three runs, exactly like every other capture in this section.
# `modpath/src/` and `modpath/run.sh` are DELIBERATELY NOT ignored: the panel head on that slide is
# a command a viewer is meant to be able to run, and a capture nobody can regenerate is not
# evidence. `modpath/src/module-info.java` carries `exports` and no `opens` on purpose - the other
# half of that slide is the `opens` the scan does not need.
c4-unit04/modpath/out/
c4-unit04/modpath/.r-*

# The long-lived project this course keeps changing. Its modules build into their own
# target/ directories, one level below the root rule.
c4-tiffinbox/tiffinbox-core/target/
c4-tiffinbox/tiffinbox-web/target/

# ============================================================================================
# Course 4 · Spring Framework Core · Section 2 "Lifecycle and Scope" (units 07-12)
# Appended, never edited. Every rule below begins `c4-`, so nothing here can reach another
# course's folder. Same shape as the Section 1 block above it, scoped per unit.
# ============================================================================================

# The isolated local repository each unit resolves into. `~/.m2/repository` and
# `~/.m2/settings.xml` are never written: every Maven run in this section carried
# -Dmaven.repo.local="$PWD/.m2-demo", with the quotes, because this tree's path has a space in
# it. `target/` is already covered at the top of this file; a nested project's is not.
c4-unit07/.m2-demo/
c4-unit08/.m2-demo/
c4-unit09/.m2-demo/
c4-unit10/.m2-demo/
c4-unit11/.m2-demo/
c4-unit12/.m2-demo/
c4-unit07/exercise/.m2-demo/
c4-unit08/exercise/.m2-demo/
c4-unit09/exercise/.m2-demo/
c4-unit10/exercise/.m2-demo/
c4-unit11/exercise/.m2-demo/
c4-unit12/exercise/.m2-demo/

# The resolved runtime classpath written by `dependency:build-classpath`, so the `java -cp`
# line in each README stays one short command. It is a file of absolute paths into .m2-demo,
# machine-local by construction, and must never be committed.
c4-unit07/.cp
c4-unit08/.cp
c4-unit09/.cp
c4-unit10/.cp
c4-unit11/.cp
c4-unit12/.cp
c4-unit07/exercise/.cp
c4-unit08/exercise/.cp
c4-unit09/exercise/.cp
c4-unit10/exercise/.cp
c4-unit11/exercise/.cp
c4-unit12/exercise/.cp

# Capture working files: one .out per named capture, the .raw behind it where a filter ran,
# and the .exit holding that run's exit code. Three of each, per capture, per the three-runs
# rule. A .raw exists wherever the run carries a java.util.logging record - either because the
# refresh was cancelled (chain.py removes that record and counts it) or because the log line
# IS the lesson (stamp.py masks the timestamp and counts what it masked).
c4-unit07/.r-*
c4-unit08/.r-*
c4-unit09/.r-*
c4-unit10/.r-*
c4-unit11/.r-*
c4-unit12/.r-*
c4-unit13/.r-*
c4-unit13/.m2-demo/
c4-unit13/cp.txt
c4-unit13/exercise/.m2-demo/
c4-unit13/exercise/cp.txt
c4-unit07/exercise/.r-*
c4-unit08/exercise/.r-*
c4-unit09/exercise/.r-*
c4-unit10/exercise/.r-*
c4-unit11/exercise/.r-*
c4-unit12/exercise/.r-*
c4-unit13/exercise/.r-*

# Second-level build output. The root `target/` rule does not reach a project nested inside a
# unit, and every exercise/ here is its own Maven project.
c4-unit07/exercise/target/
c4-unit08/exercise/target/
c4-unit09/exercise/target/
c4-unit10/exercise/target/
c4-unit11/exercise/target/
c4-unit12/exercise/target/
c4-unit13/exercise/target/

# DELIBERATELY NOT IGNORED, and this list is the point of the block: every README.md, every
# pom.xml, every src/ tree, every exercise/solution/*, `c4-unit08/lifecycle-rows.sh`,
# `c4-unit11/stamp.py`, `c4-unit13/flip.sh` and each unit's `chain.py`. A capture nobody can regenerate is not
# evidence, so the thing that produced it ships beside it.
