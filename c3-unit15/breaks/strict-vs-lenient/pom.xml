<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.tiffinbox</groupId>
  <artifactId>strict-vs-lenient</artifactId>
  <version>1.0.0</version>
  <name>Strict stubs against lenient</name>
  <description>The same unused stub under both strictness settings, in one run.</description>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>6.1.3</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>2.5.250</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>

    <!--
      Declaration ORDER matters here and it is not cosmetic. assertj-core brings
      net.bytebuddy:byte-buddy and so does mockito-core; both sit at depth 2, so Maven's
      nearest-wins rule breaks the tie by whichever was DECLARED first. Run
      `mvn dependency:tree -Dverbose` on this project and read which byte-buddy won.
    -->
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.27.7</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>5.23.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <version>5.23.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
      </plugin>

      <!--
        Resolves every dependency's file path into a property named after its coordinates,
        so the surefire <argLine> below can point -javaagent: at mockito-core's own jar
        without anybody typing a path. Declaring Mockito as a static agent this way is what
        stops it self-attaching at run time.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.11.0</version>
        <executions>
          <execution>
            <id>mockito-agent</id>
            <goals><goal>properties</goal></goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.6.0</version>
        <configuration>
          <!--
            The quotes around the property are load bearing. Surefire splits argLine on
            whitespace, and the path to the agent jar can contain a space, so without them
            the fork is launched with -javaagent:/Users/you/Downloads/Youtube and the JVM
            dies before a single test runs: "Error occurred during initialization of VM".
            This is the same one-line fix the doubles unit shipped; receipts.sh quoting is
            the regression guard that it is still here.
          -->
          <argLine>-javaagent:"${org.mockito:mockito-core:jar}"</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
